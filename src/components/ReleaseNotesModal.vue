<template>
  <!-- Release notes modal stub - can be implemented later -->
  <div v-if="false"></div>
</template>

<script setup>
import { defineProps } from 'vue'

defineProps({
  visible: Boolean
})
</script>
